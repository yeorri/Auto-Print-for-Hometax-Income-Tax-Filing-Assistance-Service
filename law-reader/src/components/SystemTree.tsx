/** 좌측 체계도 트리: 법 ↔ 시행령 ↔ 시행규칙. 노드 클릭 시 해당 법령 열기. */
export function SystemTree() {
  // TODO (Phase 1): useAppStore().systemTree 를 재귀 렌더, 클릭 → getLaw + openLaw
  return (
    <div style={{ padding: 12, color: "#888", fontSize: 13 }}>
      체계도 (법–시행령–시행규칙)
      <br />
      — Phase 1에서 구현
    </div>
  );
}
